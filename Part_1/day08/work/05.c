/*设计函数，接收两个字符串，判断第二个字符串是否为第一个字符串的子符串。
【提示】第二个字符串的内容全部在第一个字符串中*/
#include<stdio.h>
int main(int argc, char const *argv[])
{
    char a[] = "asvdf0";
    char b[] = "svd";
    return 0;
}

void isequl(char *a,int s1,char *b,int s2)
{
    for (int i=0; i < s2; i++)
    {
        if (/* condition */)
        {
            /* code */
        }
        
    }
    
}
